var typed = new Typed('#element', {
    strings: ['<i>Welcome</i> to.', 'My portofolio'],
    typeSpeed: 50,
    loop: true
  });

  function myFunction() {
    var element = document.body;
    element.classList.toggle("dark-mode");
 }